package homework;

/**
 * @author lystart
 * @create 2023-02-10 8:21
 */
public class ArrayTools {
//  声明一个数组工具类ArrayTools，包含如下方法：
//  int sum(int[] arr)：求所有元素总和
//  int max(int[] arr)：求所有元素的最大值
//  int indexOf(int[] arr, int value)：
//  查找value在arr数组中第一次出现的下标，如果不存在返回-1
//  int lastIndexOf(int[] arr, int value)：
//  查找value在arr数组中最后一次出现的下标，如果不存在返回-1
//  在测试类的main方法中，调用方法测试例如：

    int sum(int[] arr){
        int num = 0;
        System.out.println("求所有元素总和");
        for (int i = 0; i < arr.length; i++) {
            num += arr[i];
            
        }

        return 0;
    }

    int max(int[] arr){

        return 0;
    }


}
